# Bundled Resources

This folder is used to stage installer resources before `tauri build`.

- `engine_bundle/` is generated by CI/build scripts and embedded into app packages.
- Do not commit runtime-generated virtualenv contents.
